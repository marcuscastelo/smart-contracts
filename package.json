{
    "private": true,
    "version": "0.0.2",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
        "generate-types": "typechain --target=truffle-v5 'build/contracts/*.json'",
        "migrate": "tsc -p ./tsconfig.migrate.json --outDir ./migrations && truffle migrate",
        "typecheck": "truffle compile && yarn generate-types && tsc --noEmit",
        "test": "tsc -p ./tsconfig.migrate.json --outDir ./migrations && truffle test"
    },
    "dependencies": {
        "@typechain/truffle-v5": "^8.0.0",
        "typechain": "^8.0.0",
        "@types/bn.js": "^4.11.6",
        "@types/chai": "^4.2.11",
        "@types/mocha": "^7.0.2",
        "@types/web3": "^1.2.2",
        "web3-core": "^1",
        "web3-eth-contract": "^1",
        "web3-utils": "^1",
        "web3": "^1",
        "truffle": "^5.1.22",
        "typescript": "^4.6"
    },
    "devDependencies": {
        "@typechain/truffle-v5": "^8.0.0",
        "truffle-typings": "^1.0.8"
    }
}